# pi-smart-web-search

Batch web search for the pi coding agent -- ranked, readable results ready to fetch. Requires pi-smart-fetch.

固定版本：`0.4.0`；入口：`index.ts`。通过根目录 npm 依赖安装，保留上游模块目录和许可证。

来源：https://www.npmjs.com/package/pi-smart-web-search
